#include <stdio.h>
#include <string.h>

int main() {
    char palavra[100]; 
    char contrario[100];

scanf("%s", palavra);
while(palavra != "FIM"){
for(int i=(strlen(palavra)-1,j=0); i>0;i-,j++-){
contrario[j] += palavra[i];
}
if(contrario  
}
}
